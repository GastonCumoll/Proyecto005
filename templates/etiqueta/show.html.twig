{% extends 'base.html.twig' %}

{% block title %}Etiqueta{% endblock %}

{% block body %}
{% block addStyleSheets %}
    {{ parent() }}
    {{ encore_entry_link_tags('normaShow') }}
{% endblock %}
{# <header class="headerAdmin">{{ include('baseAdmin.html.twig') }}</header> #}
    <h1>Etiqueta</h1>
    
    <div class=" divBotonVolver mt-4  text-left">
        <a href={{app.request.headers.get('referer')}}><button type="button" class="btn btn-primary botones" id="botonVolver"><i class="fas fa-arrow-left"></i> Volver</button></a>
    </div>
    <br>
    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ etiquetum.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ etiquetum.nombre }}</td>
            </tr>
        </tbody>
    </table>

    {# <a href="{{ path('etiqueta_index') }}">Volver a la lista</a> #}
    <div class="container">
        <div class="divBotones mt-3 mr-2">
            <a href="{{ path('etiqueta_edit', {'id': etiquetum.id}) }}"><button type="button" class="btn btn-success botones editar" value="{{etiquetum.id}}" id="editar">Editar</button></a>
        </div>
        <div class="divBotones mt-3 mr-2">
            {{ include('etiqueta/_delete_form.html.twig') }}
        </div>
    </div>

{% endblock %}
