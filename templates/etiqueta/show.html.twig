{% extends 'base.html.twig' %}

{% block title %}Etiqueta{% endblock %}

{% block body %}
{# <header class="headerAdmin">{{ include('baseAdmin.html.twig') }}</header> #}
    <h1>Etiqueta</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ etiquetum.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ etiquetum.nombre }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('etiqueta_index') }}">Volver a la lista</a>
    <br>
    <a href="{{ path('etiqueta_edit', {'id': etiquetum.id}) }}"><button class="btn btn-info">Editar <i class="fa-regular fa-pen-to-square"></i></button> </a>

    {{ include('etiqueta/_delete_form.html.twig') }}
{% endblock %}
