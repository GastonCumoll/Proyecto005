{% extends 'base.html.twig' %}

{% block title %}Edit Etiqueta{% endblock %}

{% block body %}

{% block addStyleSheets %}
    {{ parent() }}
    {{ encore_entry_link_tags('createCss') }}
{% endblock %}
<h1>Editar Etiqueta</h1>
<div class="container text-left" >
    <div class="mt-3 p-3 border border-secondary rounded bg-light shadow mb-5 bg-white ">
    

    {{ include('etiqueta/_form.html.twig', {'button_label': 'Guardar'}) }}

    <a href="{{ path('etiqueta_index') }}" class="btn btn-primary">Cancelar</a>
    </div></div>

{% endblock %}
