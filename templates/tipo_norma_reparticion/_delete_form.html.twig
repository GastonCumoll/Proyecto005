<form method="post" action="{{ path('tipo_norma_reparticion_delete', {'id': repa.id,'t':tipoNorma.id}) }}" onsubmit="return confirm('¿Estás seguro de que quiere eliminar este item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ repa.id) }}">
    <button title="ELIMINAR" class="btn btn-danger btn-sm d-inline"><i class="fa-solid fa-trash-can">
</form>
