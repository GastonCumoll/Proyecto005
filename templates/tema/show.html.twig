{% extends 'base.html.twig' %}

{% block title %}Tema{% endblock %}

{% block body %}
    <h1>Tema</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ tema.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ tema.nombre }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('tema_index') }}">back to list</a>

    <a href="{{ path('tema_edit', {'id': tema.id}) }}">edit</a>

    {{ include('tema/_delete_form.html.twig') }}
{% endblock %}
