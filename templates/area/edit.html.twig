{% extends 'base.html.twig' %}

{% block title %}Edit Area{% endblock %}

{% block body %}

{% block addStyleSheets %}
    {{ parent() }}
    {{ encore_entry_link_tags('createCss') }}
{% endblock %}

    <h1>Editar Area</h1>

    {{ include('area/_form.html.twig', {'button_label': 'Guardar'}) }}

    <a href="{{ path('area_index') }}" class="btn btn-primary">Cancelar</a>

{% endblock %}