{% extends 'base.html.twig' %}

{% block title %}Edit Area{% endblock %}

{% block addStyleSheets %}
    {{ parent() }}
    {{ encore_entry_link_tags('createCss') }}
{% endblock %}
{% block body %}
    <h1>Editar area</h1>
    <div class="container text-left" >
        <div class="mt-3 p-3 border border-secondary rounded bg-light shadow mb-5 bg-white ">
    {{ include('area/_form.html.twig', {'button_label': 'Confirmar'}) }}
    <a href="{{ path('area_index',{'id': area.id}) }}" class="btn btn-primary mt-2 mb-2">Volver</a>
        </div></div>
    {% block javascripts %}
    {{ parent() }}
    {{encore_entry_script_tags('etiqueta')}}

{% endblock  %} 
{% endblock %}