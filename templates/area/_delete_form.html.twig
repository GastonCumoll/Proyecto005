<form method="post" action="{{ path('area_delete', {'id': area.id}) }}" onsubmit="return confirm('¿Estás seguro de que quiere eliminar este item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ area.id) }}">
    <button class="btn btn-danger btn-sm d-inline"><i class="fa-solid fa-trash-can">
</form>
