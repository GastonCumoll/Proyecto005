{% extends 'base.html.twig' %}

{% block title %}New Norma{% endblock %}

{% block body %}

{% block addStyleSheets %}
    {{ parent() }}
    {{ encore_entry_link_tags('createCss') }}
    {{ encore_entry_link_tags('select2totree') }}
{% endblock %}

{% block javascripts %}
{{ parent() }}
{{encore_entry_script_tags('select2')}}
{{encore_entry_script_tags('datepicker')}}
{{encore_entry_script_tags('etiqueta')}}
{% endblock  %} 
    <h1>Cargar norma</h1>
    <div class="container"  id="tarjeta">
        {% for flash_message in app.session.flashbag.get('verifPublicar') %}
        <div class="card shadow mb-5 rounded" id="cardAdvertencia">
            <div class="flash-notice text-white font-weight-bolder text-center">
                {{ flash_message }}
            </div>
        </div>
    {% endfor %}
        {% for flash_message in app.session.flashbag.get('archivoMaxSize') %}
        <div class="card shadow mb-5 rounded" id="cardAdvertencia">
            <div class="flash-notice text-white font-weight-bolder text-center">
                {{ flash_message }}
            </div>
        </div>
    {% endfor %}
        <div class="container">
            <h5 class="h5">Los campos con (*) son obligatorios</h5>
            
            {{ include('norma/_form.html.twig') }} 
            
        <a href="{{ path('norma_nueva') }}" class="btn btn-primary mt-2 mb-2">Volver</a>
        </div>
    </div>  


{% endblock %}