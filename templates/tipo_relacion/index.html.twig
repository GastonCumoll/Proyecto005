{% extends 'base.html.twig' %}

{% block title %}TipoRelacion index{% endblock %}

{% block body %}
{# <header class="headerAdmin">{{ include('baseAdmin.html.twig') }}</header> #}
    <h1>Tipos de relaciones</h1>

    
    <br>
    {# <div class="container">
        <div class="input-group float-right ml-auto mr-2">
        <input type="text" class="form-control col-5 float-right" id="busquedaParam" value="" placeholder="Descripcion" aria-label="Input group example" aria-describedby="btnGroupAddon2">
        <div class="input-group-prepend">
            <button class="boton btn-dark btn-sm" id="busquedaParam"><i class="fa-solid fa-magnifying-glass"></i></button>
            </div>
        </div>
    </div>
    <br>
    <br>
    <br> #}
    <div class="table-responsive">
    <table class="table table-hover ">
        <thead class="table-active">
            <tr>
                <th>{{ knp_pagination_sortable(tipo_relacions, 'Id', 'p.id') }}</th>
                <th>{{knp_pagination_sortable(tipo_relacions, 'Descripcion', 'p.nombre')}}</th>
                <th>Opciones</th>
            </tr>
        </thead>
        <tbody>
            {% for tipo_relacion in tipo_relacions %}
            <tr>
                <td>
                    {{tipo_relacion.id}}
                </td>
                <td>
                    {{tipo_relacion.nombre}}
                </td>
                <td>
                    <a  href="{{path('tipo_relacion_show', {'id': tipo_relacion.id})  }}"><button class="btn btn-info btn-sm"<i class="far fa-eye"></i></button></a>
                    <a  href="{{path('tipo_relacion_edit', {'id': tipo_relacion.id})  }}"><button class="btn btn-info btn-sm"><i class="fa-solid fa-pen-to-square"></i></button></a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no se encontraron registros</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
    <div class="navigation">
        {{ knp_pagination_render(tipo_relacions) }}
    </div>
    <a href="{{ path('tipo_relacion_new') }}"> <button  type="button" class="btn btn-info btn-sm">Agregar <i class="fa-solid fa-plus"></i></button> </a>
</div>

{% endblock %}
