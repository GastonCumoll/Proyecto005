{% extends 'base.html.twig' %}

{% block title %}New TipoNormaRol{% endblock %}

{% block body %}

{% block addStyleSheets %}
    {{ parent() }}
    {{ encore_entry_link_tags('createCss') }}
{% endblock %}

<h1 class="font-weight-bold text-center">Cargar nuevo rol a {{tipoNorma.nombre}}</h1>
<div class="container text-left" >
    <div class="mt-3 p-3 border border-secondary rounded bg-light shadow mb-5 bg-white ">
        {{ include('tipo_norma_rol/_form.html.twig') }}
        <a href="{{ path('rol_tipo_norma',{'id':tipoNorma.id}) }}" class="btn btn-primary">Cancelar</a> 
    </div>
        
</div>

{% block javascripts %}
    {{ parent() }}
{{encore_entry_script_tags('etiqueta')}}
{% endblock  %} 
{% endblock %}
