{% extends 'base.html.twig' %}

{% block title %}Edit TipoNormaRol{% endblock %}
{% block addStyleSheets %}
    {{ parent() }}
    {{ encore_entry_link_tags('createCss') }}
{% endblock %}
{% block body %}
    <h1>Cambiar el rol a {{tipoNorma.nombre}}</h1>
    <div class="container text-left" >
        <div class="mt-3 p-3 border border-secondary rounded bg-light shadow mb-5 bg-white ">
    {{ include('tipo_norma_rol/_form.html.twig', {'button_label': 'Confirmar'}) }}
    <a href="{{ path('rol_tipo_norma',{'id': tipoNorma.id}) }}" class="btn btn-primary mt-2 mb-2">Volver</a>
        </div></div>
    {% block javascripts %}
    {{ parent() }}
    {{encore_entry_script_tags('etiqueta')}}

{% endblock  %} 
{% endblock %}
