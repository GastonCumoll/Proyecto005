{% extends 'base.html.twig' %}

{% block title %}TipoConsulta index{% endblock %}

{% block body %}
    <h1>TipoConsulta index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Nombre</th>
                <th>Email</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for tipo_consultum in tipo_consultas %}
            <tr>
                <td>{{ tipo_consultum.id }}</td>
                <td>{{ tipo_consultum.nombre }}</td>
                <td>{{ tipo_consultum.email }}</td>
                <td>
                    <a href="{{ path('tipo_consulta_show', {'id': tipo_consultum.id}) }}">show</a>
                    <a href="{{ path('tipo_consulta_edit', {'id': tipo_consultum.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('tipo_consulta_new') }}">Create new</a>
{% endblock %}
