{% extends 'base.html.twig' %}

{% block title %}Edit TipoConsulta{% endblock %}

{% block addStyleSheets %}
    {{ parent() }}
    {{ encore_entry_link_tags('createCss') }}
{% endblock %}
{% block body %}
    <h1>Editar tipo de consulta</h1>
    <div class="container text-left" >
        <div class="mt-3 p-3 border border-secondary rounded bg-light shadow mb-5 bg-white ">
    {{ include('tipo_consulta/_form.html.twig', {'button_label': 'Guardar'}) }}
    <a href="{{ path('tipo_consulta_index') }}" class="btn btn-primary mt-2 mb-2">Volver</a>
        </div></div>
    {% block javascripts %}
    {{ parent() }}
    {{encore_entry_script_tags('etiqueta')}}

{% endblock  %} 


{% endblock %}
