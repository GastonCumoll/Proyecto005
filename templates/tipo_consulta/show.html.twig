{% extends 'base.html.twig' %}

{% block title %}TipoConsulta{% endblock %}

{% block body %}
    <h1>TipoConsulta</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ tipo_consultum.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ tipo_consultum.nombre }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ tipo_consultum.email }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('tipo_consulta_index') }}">back to list</a>

    <a href="{{ path('tipo_consulta_edit', {'id': tipo_consultum.id}) }}">edit</a>

    {{ include('tipo_consulta/_delete_form.html.twig') }}
{% endblock %}
