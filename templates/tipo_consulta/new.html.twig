{% extends 'base.html.twig' %}

{% block title %}New TipoConsulta{% endblock %}

{% block body %}
{% block addStyleSheets %}
    {{ parent() }}
    {{ encore_entry_link_tags('createCss') }}
{% endblock %}
    <h1 class="font-weight-bold text-center">Cargar tipo de consulta</h1>
    <div class="container text-left" >
        <div class="mt-3 p-3 border border-secondary rounded bg-light shadow mb-5 bg-white ">

        {{ include('tipo_consulta/_form.html.twig') }}
        <a href="{{ path('tipo_consulta_index') }}" class="btn btn-primary">Volver</a>
    </div>
    </div>
    {% block javascripts %}
    {{ parent() }}
    {{encore_entry_script_tags('tipoConsultaNew')}}
{% endblock  %} 
{% endblock %}
