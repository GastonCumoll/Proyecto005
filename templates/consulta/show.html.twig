{% extends 'base.html.twig' %}

{% block title %}Consulta{% endblock %}

{% block body %}
    <h1>Consulta</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ consultum.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ consultum.nombre }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ consultum.email }}</td>
            </tr>
            <tr>
                <th>NumeroTel</th>
                <td>{{ consultum.numeroTel }}</td>
            </tr>
            <tr>
                <th>Texto</th>
                <td>{{ consultum.texto }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('consulta_index') }}">back to list</a>

    <a href="{{ path('consulta_edit', {'id': consultum.id}) }}">edit</a>

    {{ include('consulta/_delete_form.html.twig') }}
{% endblock %}
