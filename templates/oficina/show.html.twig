{% extends 'base.html.twig' %}

{% block title %}Oficina{% endblock %}

{% block body %}
    <h1>Oficina</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ oficina.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ oficina.nombre }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('oficina_index') }}">back to list</a>

    <a href="{{ path('oficina_edit', {'id': oficina.id}) }}">edit</a>

    {{ include('oficina/_delete_form.html.twig') }}
{% endblock %}
