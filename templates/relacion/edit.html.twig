{% extends 'base.html.twig' %}

{% block title %}Edit Relacion{% endblock %}

{% block body %}

{% block addStyleSheets %}
    {{ parent() }}
    {{ encore_entry_link_tags('createCss') }}
{% endblock %}
    <h1>Editar Relacion</h1>
    
    <div class="container"  id="tarjeta">
        <div class="container">
    {{ include('relacion/_form.html.twig', {'button_label': 'Guardar'}) }}

    <a href={{app.request.headers.get('referer')}} class="btn btn-primary">Volver</a>
        </div>
    </div>
{% endblock %}
