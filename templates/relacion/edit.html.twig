{% extends 'base.html.twig' %}

{% block title %}Edit Relacion{% endblock %}

{% block body %}

{% block addStyleSheets %}
    {{ parent() }}
    {{ encore_entry_link_tags('createCss') }}
{% endblock %}
    <h1>Edit Relacion</h1>
    <div class="container text-left" >
        <div class="mt-3 p-3 border border-secondary rounded bg-light shadow mb-5 bg-white ">
    {{ include('relacion/_form.html.twig', {'button_label': 'Guardar'}) }}

    <a href={{app.request.headers.get('referer')}} class="btn btn-primary">Cancelar</a>
        </div></div>
{% endblock %}
