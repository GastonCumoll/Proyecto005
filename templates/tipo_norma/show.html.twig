{% extends 'base.html.twig' %}

{% block title %}TipoNorma{% endblock %}

{% block body %}
{% block addStyleSheets %}
    {{ parent() }}
    {{ encore_entry_link_tags('normaShow') }}
{% endblock %}
{# <header class="headerAdmin">{{ include('baseAdmin.html.twig') }}</header> #}

    <h1>Tipo de norma</h1>

    <div class=" divBotonVolver mt-4  text-left">
        <a href={{app.request.headers.get('referer')}}><button type="button" class="btn btn-primary botones" id="botonVolver"><i class="fas fa-arrow-left"></i> Volver</button></a>
    </div>
    <br>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ tipo_norma.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ tipo_norma.nombre }}</td>
            </tr>
        </tbody>
    </table>
    <div class="container">
        <div class="divBotones mt-3 mr-2">
            <a href="{{ path('tipo_norma_edit', {'id': tipo_norma.id}) }}"><button type="button" class="btn btn-success botones editar" value="{{tipo_norma.id}}" id="editar">Editar</button></a>
        </div>
        <div class="divBotones mt-3 mr-2">
            {{ include('tipo_norma/_delete_form.html.twig') }}
        </div>
    </div>

{% endblock %}
