<form method="post" action="{{ path('tipo_norma_delete', {'id': tipo_norma.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ tipo_norma.id) }}">
    <button class="btn btn-danger btn-sm d-inline"><i class="fa-solid fa-trash-can">
</form>
