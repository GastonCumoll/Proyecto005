{% extends 'base.html.twig' %}

{% block addStyleSheets %}
    {{ parent() }}
    {{ encore_entry_link_tags('createTipoNorma') }}
{% endblock %}

{% block title %}Norma index{% endblock %}

{% block body %}

<h1 class="font-weight-bold text-center">Tipo de norma a cargar</h1>
    <div class="container text-center" >
        <div class="mt-3 p-3 border border-secondary rounded bg-light shadow mb-5 bg-white ">
            {% for tipo_norma in tipo_normas %}
                {% for unaNormaUsuario in normasUsuario %}
                    {% if unaNormaUsuario == tipo_norma.id%}
                        <a href="{{ path('norma_new', {'id': tipo_norma.id}) }}" class="btn btn-primary btn-boton mt-3" >{{tipo_norma.nombre}}</a>
                        <br>
                    {% endif %}
                {% endfor %}
            {% endfor %}
</div>
</div>
    {# <h2 class ="mb-4">
        
    </h2>
    {% for tipo_norma in tipo_normas %}
    <a href="{{ path('norma_new', {'id': tipo_norma.id}) }}" class="btn btn-primary btn-lg" role="button" aria-pressed="true">{{tipo_norma.nombre}}</a>
    {% endfor %}
</div> #}
    
    {% endblock %}