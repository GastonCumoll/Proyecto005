{% extends 'base.html.twig' %}

{% block addStyleSheets %}
    {{ parent() }}
    {{ encore_entry_link_tags('createTipoNorma') }}
{% endblock %}

{% block title %}Norma index{% endblock %}

{% block body %}

<h1 id="tituloH1">Tipo de norma a cargar</h1>
    <div class="container text-center" >
        <div class="card">
            {% for tipo_norma in tipo_normas %}
                {% for unaNormaUsuario in normasUsuario %}
                    {% if unaNormaUsuario == tipo_norma.nombre %}
                        <a href="{{ path('norma_new', {'id': tipo_norma.id}) }}" class="btn btn-primary" id="normaNueva" >{{tipo_norma.nombre}}</a>
                    {% endif %}
                {% endfor %}
            {% endfor %}
</div>
</div>
    {# <h2 class ="mb-4">
        
    </h2>
    {% for tipo_norma in tipo_normas %}
    <a href="{{ path('norma_new', {'id': tipo_norma.id}) }}" class="btn btn-primary btn-lg" role="button" aria-pressed="true">{{tipo_norma.nombre}}</a>
    {% endfor %}
</div> #}
    
    {% endblock %}