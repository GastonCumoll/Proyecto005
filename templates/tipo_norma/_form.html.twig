{{ form_start(form,{'attr':{'id':'form_tipo_norma'}}) }}
    {{ form_widget(form) }}
    <button class="btn btn-success" id="boton_guardar">{{ button_label|default('Guardar') }}</button>
{{ form_end(form) }}
