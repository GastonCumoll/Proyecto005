{% extends 'base.html.twig' %}

{% block title %}Item index{% endblock %}

{% block body %}


{# <header class="headerAdmin">{{ include('baseAdmin.html.twig') }}</header> #}
<h1>Índice</h1>

<br>
<div class="container">
    <div class="input-group float-right ml-auto mr-2">
    <input type="text" class="form-control col-5 float-right" id="busquedaParam" value="" placeholder="Nombre" aria-label="Input group example" aria-describedby="btnGroupAddon2">
    <div class="input-group-prepend">
        <button class="boton btn-info btn-sm" id="btnBusquedaParam"><i class="fa-solid fa-magnifying-glass"></i></button>
        </div>
    </div>
</div>
<br>
    <br>
    <br>
<div class="table-responsive">
    <table class="table table-hover ">
        <thead class="table-active" >
        <tr>
            <th>{{ knp_pagination_sortable(items, 'Id', 'p.id') }}</th>
            <th>{{knp_pagination_sortable(items, 'Nombre', 'p.nombre')}}</th>
            {% if rol == "DIG_ADMINISTRADOR" %}
            <th>Opciones</th>
            {% endif %}
            

        </tr>
    </thead>
    <tbody>
        {% for item in items %}
        <tr>

            <td>
                {{item.id}}
            </td>
            <td>
                {{ item.nombre }}
            </td>
            {% if rol == "DIG_ADMINISTRADOR" %}
            <td>
                <a  href="{{path('item_edit', {'id': item.id})  }}"><button class="btn btn-info btn-sm d-inline-block float-left mr-1"><i class="fa-solid fa-pen-to-square"></i></button></a>
                {{ include('item/_delete_form.html.twig') }}
            </td>
            {% endif %}
            

        </tr>
    {% else %}
        <tr>
            <td colspan="3">no se encontraron registros</td>
        </tr>
    {% endfor %}
    </tbody>
</table>
<div class="navigation">
    {{ knp_pagination_render(items) }}
</div>
</div>
<br>
{% if rol == "DIG_ADMINISTRADOR" %}

<a href="{{ path('item_new') }}"> <button  type="button" class="btn btn-info btn-sm">Agregar <i class="fa-solid fa-plus"></i></button> </a>
<br>
{% endif %}
{% block javascripts %}
    {{encore_entry_script_tags('busquedaParamItem')}}
    {% endblock %}
{% endblock %}
