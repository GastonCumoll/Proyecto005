{% extends 'base.html.twig' %}

{% block title %}New Item{% endblock %}

{%  block body %}

{% block addStyleSheets %}
    {{ parent() }}
    {{ encore_entry_link_tags('createCss') }}
{% endblock %}
<h1>Cargar nuevo índice</h1>
<div class="container text-left" >
    <div class="mt-3 p-3 border border-secondary rounded bg-light shadow mb-5 bg-white ">
        
    {{ include('item/_form.html.twig') }}
    <a href="{{ path('item_index') }}" class="btn btn-primary">Volver</a>
</div>
</div>
{% block javascripts %}
{{ parent() }}
{# {{encore_entry_script_tags('etiquetasNorma')}} #}
{{encore_entry_script_tags('datepicker')}}
{{encore_entry_script_tags('etiqueta')}}

{% endblock  %} 
{% endblock %}