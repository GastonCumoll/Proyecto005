{% extends 'base.html.twig' %}

{% block title %}Change Password{% endblock %}

{% block body %}

    {% block addStyleSheets %}
        {{ parent() }}
        {{ encore_entry_link_tags('createCss') }}
    {% endblock %}
    <h1 class="font-weight-bold text-center">Cambiar contraseña</h1>
        {% for flash_message in app.session.flashbag.get('notice') %}
            <div class="card shadow mb-5 rounded" id="cardAdvertencia">
                <div class="flash-notice text-white font-weight-bolder text-center">
                    {{ flash_message }}
                </div>
            </div>
        {% endfor %}
    <div class="container text-left" >
        <div class="mt-3 p-3 border border-secondary rounded bg-light shadow mb-5 bg-white ">

        {{ include('general/_formChangePass.html.twig') }}
        <a href="{{ path('inicio_admin') }}" class="btn btn-primary">Cancelar</a>
    </div>
    </div>

        {% block javascripts %}
            {{ parent() }}
        {% endblock %}
    {% endblock %}
